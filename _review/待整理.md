实现一个EventListener类，包含on，off，emit方法

写一个观察者模式

判断一个二叉树中，是否有某条路径的节点和为n

生成器函数：generator函数

Promise

什么是预编译？预编译发生了什么？什么时候会进行变量提升？

js事件循环

js文件执行时，浏览器渲染发生在什么时候？浏览器有哪些线程？

共享原型的继承模式和圣杯继承，两者的缺点

受控组件和非受控组件的区别

redux工作流程

手写代码，createStore源码

深度优先遍历二叉树，用递归和递归两种方式

链表的反转，每隔k位反转链表

数组扁平化

对象扁平化

写一个简单的闭包

判断数组方法

webpack命令

sort排序内部如何排序的

instanceof原理

懒加载如何实现，预先加载如何实现

手写防抖节流

var a = {x: 1}; var b = a; a = a.x = { x: 1 };console.log(a, b);
a: {x: 1}, b: {x: {x: 1}}

var let const 的区别

position的值，sticky不设置这个定位通过什么可以实现

写一个二叉树的插入

二分查找

找为k值的路径

模块化的方式

css和js是串行还是并行

进程和线程

基本数据类型

webpack原理，为什么可以import css文件

VM了解多少，原理明白么？

ES7了解么

react开发用的hook么？不是的话说一下生命周期

redux开发

react diff算法？

async-await

redux原理

水平垂直居中的方案

如何实现大文件传输，如何校验文件正确与否

CommonJS和ES Module有何区别

浏览器事件循环

从输入URL到显示页面发生了什么

如何求出两个相交链表的交叉点

走台阶问题

实现一个bind方法

requestAnimationFrame和setTimeout的区别

React的Context是什么，应用场景

React中数据的流向

简述事件委托机制

前端路由的实现方法

应用埋点如何检测pushState

ReactNative中JS如何与系统交互

接触过WebGL吗，是什么

接触过Canvas么

一行代码完成事件字符串转秒数

扫码登录的原理

LeetCode 001. Two Sum
LeetCode 015. 3Sum (可能会问 LeetCode 18. 4Sum 思路)
LeetCode 020. Valid Parentheses
LeetCode 021. Merge Two Sorted Lists
LeetCode 025. Reverse Nodes in k-Group
LeetCode 053. Maximum Subarray
LeetCode 066. Plus One（等价于：高精度加法）
LeetCode 098. Validate Binary Search Tree
LeetCode 110. Balanced Binary Tree
LeetCode 134. Gas Station
LeetCode 136. Single Number
LeetCode 137. Single Number II
LeetCode 146. LRU Cache（变形题：带有过期时间的 LRU 缓存）
LeetCode 206. Reverse Linked List
LeetCode 215. Kth Largest Element in an Array（等价于：快速排序）
LeetCode 232. Implement Queue using Stacks
LeetCode 328. Odd Even Linked List
LeetCode 415. Add Strings（等价于：大数加法）
LeetCode 470：rand7() rand10()
LeetCode 496. Next Greater Element I（时间复杂度O(n)）
LeetCode 716. Max Stack（两个栈实现最大栈，要求 pop，push，get_max 都为O(1)）
LeetCode 860. Lemonade Change
LeetCode 862. Shortest Subarray with Sum at Least K
LeetCode 876. Middle of the Linked List
LeetCode 946. Validate Stack Sequences

跨域怎么解决？跨域携带cookie，简述所有状态吗。简述闭包

手写call、bind、apply内部实现

写函数实现1秒钟输出一个数组

一个混this和原型的程序 问输出

如何判断一个对象是空对象

算法 最大连续子序列和dp

手写防抖、节流

手写正则表达式判断电话号码

如何判断一个对象是不是array

手写快排

手写斐波那契，递归，动态规划

找数组里综合大于等于目标值的连续数字合

promise封装原生ajax

找二叉树从根节点到叶节点所有路径的总连续和

用函数实现一个类以及继承

new的过程

判断类型typeof、instanceof、toString

nodejs任务队列事件循环

promise写一个delay函数

happypack原理【进程/线程、区别】

React 的loadable核心，import（）本质上和React.lazy和Suspense原理相似

dynamic import（loadable原理之一）

为前端路由设计 多个SPA根据URL变化改变 路由解决方案

React.memo原理

fibonacci函数，之后进行memory优化

redux context的区别和使用场景

hook的限制，hook能否用于受控组件【ref】

如何进行图片压缩

常见处理文件的loader【file-loader,url-loader】

koa的原理与express的对比

快排 原理和优化 分析复杂度

HMR热更新原理

双向绑定原理

二叉树前序中序，生成后序

非js写的nodejs模块是如何使用nodejs调用的【代码转换过程】

Mysql的存储引擎

做过哪些前端的性能优化实践

做过哪些平台进行性能测试【lighthouse】

mongoose有什么作用

判断一个对象是不是空对象

getUrlParams(url, key)

说一说你了解的express中间件

Fiber架构，调度原理

koa2洋葱模型

commander.js && inquirer.js

第i次执行输出i
var getNum = (function () {
    let i = 0;
    return function() {
        ++i
        console.log(i);
    }
})();

setState为什么以及何时是异步的
【为了性能优化进行的批量更新】

css3动画，GPU加速

webpack的工作流程

用过哪些loader和plugin

commonJS和ES6模块化区别，（tree shaking）

白屏优化

redux的原理 dispatch store

链表反转、数组反转

module.export exports export的区别

判断空对象为空的方法，大约是写出隐式转换的底层代码？

request.animation.frame

react hoc

ssl握手过程 对称加密 非对称加密

箭头函数的this

node事件循环

数据库索引

fs读取文件转buffer

打包的性能优化【业务依赖分开，依赖做缓存】

cli【无法判断操作系统而准去的执行响应的shell命令】

GraphQL？

typeof能检测8种值，string，boolean，number，function，object，undefined，symbol，bigint

https://www.nowcoder.com/discuss/177482?type=post&order=time&pos=&page=3&subType=2

PureComponent

节流

function throttle(fn, time) {
    var canRun = true;
    return function() {
        if (!canRun) return false;
        canRun = false;
        var that = this, args = arguments.slice();
        setTimeout(function() {
            fn.apply(that, args);
            canRun = true;
        }, time || 500);
    }
}
setInterval(throttle(function() {
    console.log('hello world');
}), 100)

reduce实现map
Array.prototype.map = function(callback) {
    var arr = this;
    return arr.reduce((acc, cur, i) => {
        acc.push(callback(cur, i, arr));
        return acc;
    })
}

二叉树的后序遍历
function TreeNode(val) {
    this.val = val;
    this.left = null;
    this.right = null;
}
function visit(root) {
    if (root) {
        visit(root.left);
        visit(root.right);
        console.log(root.val);
    }
}
非递归版本？

nodejs的事件循环


对于大部分图片：
<img async decoding="async" loading="lazy" importance="low" />
可以优化 FMP。

